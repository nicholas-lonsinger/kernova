# Xcode
build/
DerivedData/
artifacts/

# Per-developer signing team, hand-set in Config/Local.xcconfig — see
# Config/Base.xcconfig.
Config/Local.xcconfig
# Written next to the .xcodeproj by Xcode's Relative Derived Data setting
# (see docs/BUILD.md); the suffix is a per-install identifier, hence the glob.
.derived-data-log-*
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint
*.xcworkspace/xcuserdata/

# Swift Package Manager
.build/
Packages/
Package.resolved
*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/configuration/
!*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

# CocoaPods
Pods/

# Carthage
Carthage/Build/

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
*.swp
*.swo
*~
.idea/
.vscode/
# sourcekit-lsp build context, written per checkout by `make install-lsp`; the
# config pins an absolute build root, so it never travels between checkouts.
buildServer.json
.compile

# Claude Code
.claude/*.local.*
.claude/worktrees/
.claude/scheduled_tasks.lock
CLAUDE.local.md

# Secrets
*.env
*.pem
*.p12
*.key
*.profraw
